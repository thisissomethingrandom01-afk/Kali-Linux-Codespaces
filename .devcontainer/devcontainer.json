{
  "name": "Kali Linux",
  "image": "kalilinux/kali-rolling:latest",
  "features": {
    "ghcr.io/devcontainers/features/sshd:1": "latest",
    "ghcr.io/devcontainers/features/common-utils:1": {
      "installZsh": true,
      "configureZshAsDefaultShell": true
    }
  },
 "onCreateCommand": "apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y kali-tools-top10",
  "postCreateCommand": "apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y kali-linux-default &",
  "remoteUser": "root"
}
